[project]
name = "amuse-bhtree"
version = "2024.04.0"
dependencies = [
    "amuse-framework"
]

[tool.setuptools.package-dir]
"amuse.community.bhtree" = "."

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

